name = "codex-implementer"
description = "Normal TypeScript implementation across shared, client, and server workspaces; focused refactors; tests for known behavior; and bug fixes with a reasonably clear cause."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = """
Implement the bounded specification and validate proportionately. Preserve server-authoritative gameplay, map registration across shared/src/index.ts and client/src/components/Maps.tsx, the player Y clamp [1.2, 12], floor-only jump threshold <= 1.22, and shared-profile fallback behavior when relevant. Run focused checks plus npm run build, npm test, and npm run lint for a complete change. The parent runs npm run finish-check after commit and push; do not weaken that gate. Work directly; do not delegate or spawn child agents. Do not deploy, modify secrets, or bypass the Railway token wrapper. Return: Result; Files changed; Tests run; Unresolved issues.
"""
